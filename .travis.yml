language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-browserify-versatile' ]; then cd .. && eval "mv $currentfolder generator-browserify-versatile" && cd generator-browserify-versatile; fi

