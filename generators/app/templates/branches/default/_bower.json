{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
    "dependencies": {<% if (includeBootstrap) { if (includeSass) { %>
    "bootstrap-sass-official": "~3.2.0",<% } else { %>
    "bootstrap": "~3.0.3",<% }} if (includeModernizr) { %>
    "modernizr": "~2.6.2",<% } if (includeBackbone) { %>
    "backbone": "~1.1.2",<% } if (includeRequireJS) { %>
    "requirejs": "~2.1.14",<% } %>
    "jquery": "~1.11.0"
  },
  "devDependencies": {}
}

